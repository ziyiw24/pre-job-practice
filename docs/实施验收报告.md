# M1–M5 实施验收报告

## 仓库内验收

| 里程碑 | 证据 | 结论 |
|---|---|---|
| M1 Provider / Agent | `app/application/ports/model_gateway.py`、`app/agents/training/graph.py`、Agent 分支测试 | 通过 |
| M1 评测 | 20 份 fixture，baseline/agent 各 100 题，quote/number 均 100% | mock 指标通过；人工语义指标待真实模型 |
| M3 文档 | PDF/DOCX 签名、大小、页数、加密/扫描错误；block/页码重定位；审核与高风险确认 | 通过 |
| M4 多用户 | JWT、owner/manager/employee、store_id 权限、MySQL migration/仓储、发布/分配/答题/成绩 | 通过 |
| M4 反作弊 | 员工提交前 API 不返回 `answer`/`explanation`，成绩只由服务端读取标准答案计算 | 通过 |
| M5 安全 | 生产配置拒绝内存存储/弱 JWT/通配 CORS；请求体、限流、模型预算、脱敏日志 | 通过 |
| M5 隐私 | COS 私有 key、文档软删除、账号删除/投诉申请入口 | 通过 |
| 工程基线 | Python 3.11，`167 passed`，TypeScript、H5、weapp、`git diff --check` | 通过（H5 仅有 307 KiB 体积警告） |

交付审计已补齐：统一模型网关生成报告、MySQL 文档缓存恢复、删除态全链路阻断、重复交卷连接安全、Redis 幂等原子写，以及 MySQL/Redis/对象存储就绪检查和 Agent 节点耗时日志。

## 不能在本地伪造的外部验收

- 真实模型质量、token、延迟与成本：需项目所有者提供 Key。
- Redis/MySQL/COS 真实容灾与恢复：需云资源。
- 微信真机、合法域名、ICP、类目、隐私声明和审核：需 AppID 和运营主体。
- 店长人工标注：按用户指示跳过 M2，不用 mock 数据冒充真实可接受率。

外部条件的具体操作见 `docs/生产交接清单.md`。
